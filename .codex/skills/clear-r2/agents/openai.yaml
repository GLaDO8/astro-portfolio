interface:
  display_name: "Clear Health R2"
  short_description: "Clear the private health R2 bucket in one command"
  default_prompt: "Use $clear-r2 to immediately clear the hardcoded private health R2 bucket."
