---
import Accordion from "./Accordion";

interface Props {
	title: string;
	subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<Accordion title={title} subtitle={subtitle} client:load>
    <slot />
</Accordion>
